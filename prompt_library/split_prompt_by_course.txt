请将用户提示词按照课程信息分割，不同分局请用句号分割。
同样，如果后一句中存在对前一句的修饰（如：不排）请加上。与排课不相关的信息无需总结。

用户提示词：初三初一美术同一节课最多2个班。

hint：

	1.	不拆分“初三初一”一类的年级信息，每个分句中都要包含完整的年级组合信息。
	2.	课程信息“美术”也不拆分，即使有多个年级组合，仍然保持在同一句话中不被分割。
	3.	返回的分句应尽可能保持与原句结构相似，只在必要时进行拆分。

预期回答： 初三初一美术同一节课最多2个班。

用户提示词：初一初二初三语文和数学同一节课最多3个班

hint：

	1.	不拆分“初一初二初三”一类的年级信息，每个分句中都要包含完整的年级组合信息。
	2.	课程信息“语文”和“数学”可分开，但要保证每个分句包含完整的课程信息和年级组合。
	3.	返回的分句应尽可能保持与原句结构相似，只在必要时进行拆分。

预期回答： 初一初二初三语文同一节课最多三个班。初一初二初三数学同一节课最多三个班。

